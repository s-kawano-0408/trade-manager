# 設計書 - Trade Manager

## 1. システム構成図

```
┌──────────────────────────────────────────┐
│  トレードデータの入力（2つの方法）           │
│                                          │
│  ① 手入力フォーム（画面から1件ずつ入力）    │
│  ② MT4 HTMLインポート（一括取り込み）       │
└────────────────┬─────────────────────────┘
                 │ REST API
                 ↓
          ┌───────────┐      SQL       ┌───────────┐
          │  FastAPI   │ ─────────────→ │ Supabase  │
          │ (Python)   │   SQLAlchemy   │(PostgreSQL)│
          └───────────┘                └───────────┘
                 ↑                           ↑
                 │ REST API                  │
                 │                           │
           ┌───────────┐                    │
           │  React    │ ───── 読み取り ────┘
           │(Frontend) │   （API経由のみ）
           └───────────┘
                 │
                 │ API呼び出し（ボタン押下時のみ）
                 ↓
           ┌───────────┐
           │ Claude API │
           │(Anthropic) │
           └───────────┘
```

---

## 1.5 トレードデータの入力方法

### 背景

Mac版MT4はEA（自動プログラム）でのWebhook送信が現実的でないため、以下の2つの方法でデータを入力する。

### ① 手入力

- 画面のフォームからトレードを1件ずつ入力
- 入力項目：決済日時・売買方向・Lot・エントリー価格・決済価格・損益・手数料・スワップ
- `ticket` 番号はシステムが自動生成（タイムスタンプベース）

### ② MT4 HTMLインポート

MT4の画面から口座履歴をHTMLで書き出して、まとめて取り込む方法。

```
MT4「ターミナル」→「口座履歴」→ 右クリック →「レポートを保存」
      ↓ HTMLファイルを書き出す
画面の「HTMLインポート」ボタンからファイルを選択
      ↓
FastAPI がHTMLをパース
      ↓
Supabase に保存
```

### HTMLインポート時の上書きルール

HTMLに含まれる日付を基準に、その日のデータをまるごと置き換える。

- HTMLに「2026/03/07」のデータが10件 → 同日の手入力データを削除して10件で上書き
- HTMLに含まれない日付のデータはそのまま残す

→ **HTMLが常に正の情報とみなす**

---

## 2. フロントエンド設計

### 2.1 ディレクトリ構成

```
frontend/src/
├── components/              # 共通UIパーツ
│   ├── layout/
│   │   ├── Sidebar.tsx      # サイドバー（折りたたみ対応）
│   │   ├── Header.tsx       # ヘッダー
│   │   └── Layout.tsx       # レイアウト全体の枠組み
│   └── ui/                  # shadcn/ui コンポーネント（自動生成）
├── pages/                   # 各画面
│   ├── Dashboard.tsx        # ダッシュボード
│   ├── TradeList.tsx        # トレード一覧
│   ├── AiAnalysis.tsx       # AI分析
│   ├── EconomicCalendar.tsx # 経済指標カレンダー
│   └── TradeDiary.tsx       # トレード日記
├── hooks/                   # カスタムフック
│   ├── useTrades.ts         # トレードデータ取得
│   └── useDiary.ts          # 日記データ取得
├── lib/
│   ├── utils.ts             # ユーティリティ（shadcn/ui）
│   └── api.ts               # API通信の共通関数
├── types/
│   └── index.ts             # TypeScript型定義
├── App.tsx                  # ルーティング定義
├── main.tsx                 # エントリーポイント
└── index.css                # グローバルCSS・Tailwind
```

### 2.2 ルーティング

| パス | ページ | 説明 |
|------|--------|------|
| `/` | - | `/dashboard` にリダイレクト |
| `/dashboard` | Dashboard | ダッシュボード |
| `/trades` | TradeList | トレード一覧 |
| `/ai-analysis` | AiAnalysis | AI分析 |
| `/economic-calendar` | EconomicCalendar | 経済指標カレンダー |
| `/diary` | TradeDiary | トレード日記 |

### 2.3 レイアウト構成

```
┌──────────────────────────────────────────────┐
│  [☰]  TRADE MANAGER              2026/03/01  │  ← Header
├──┬───────────────────────┬───────────────────┤
│📊│                       │                   │
│📋│   メインパネル        │  サブパネル       │  ← 各ページの
│🤖│   （グラフ・テーブル） │  （統計・補助）   │    コンテンツ
│📅│                       │                   │
│📝│                       │                   │
│  │                       │                   │
└──┴───────────────────────┴───────────────────┘
 ↑ Sidebar（折りたたみ対応）
```

- サイドバー：通常時アイコンのみ、ホバーまたはクリックで展開
- ヘッダー：アプリ名 + 日付表示
- メインエリア：ページごとにパネル分割数が異なる

---

## 3. バックエンド設計

### 3.1 ディレクトリ構成

```
backend/
├── main.py                  # FastAPIエントリーポイント
├── routers/
│   ├── trades.py            # トレードAPI
│   ├── diary.py             # 日記API
│   └── analysis.py          # AI分析API
├── models/
│   ├── trade.py             # トレードテーブル定義
│   └── diary.py             # 日記テーブル定義
├── schemas/
│   ├── trade.py             # トレードのバリデーション
│   ├── diary.py             # 日記のバリデーション
│   └── analysis.py          # 分析リクエスト/レスポンス
├── services/
│   └── ai_analysis.py       # Claude API呼び出しロジック
├── database.py              # DB接続設定
├── .env                     # 環境変数
└── requirements.txt         # Pythonパッケージ一覧
```

### 3.2 API設計

#### トレード関連

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/api/trades` | トレード一覧を取得 |
| POST | `/api/trades/manual` | 手入力でトレードを1件登録 |
| POST | `/api/trades/import` | MT4のHTMLファイルを一括インポート（日付単位で上書き） |
| GET | `/api/trades/summary` | 指定期間のサマリー（勝率・損益等）を取得 |

#### 日記関連

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/api/diary` | 日記一覧を取得 |
| GET | `/api/diary/{date}` | 特定日の日記を取得 |
| POST | `/api/diary` | 日記を新規作成 |
| PUT | `/api/diary/{id}` | 日記を更新 |
| DELETE | `/api/diary/{id}` | 日記を削除 |

#### AI分析関連

| メソッド | パス | 説明 |
|---------|------|------|
| POST | `/api/analysis` | 指定期間のAI分析を実行 |

#### HTMLインポートの上書きルール

```
POST /api/trades/import にHTMLファイルを送信
  ↓
HTMLに含まれる日付を一覧取得
  ↓
その日付に一致する既存レコードを全削除
  ↓
HTMLのデータをINSERT
  ↓
HTMLに含まれない日付のデータはそのまま
```

---

## 4. データベース設計

### 4.1 ER図

```
┌─────────────────────────┐
│        trades            │
├─────────────────────────┤
│ id          SERIAL PK    │
│ ticket      INTEGER UK   │  ← MT4のチケット番号（一意）
│ symbol      VARCHAR(20)  │
│ type        INTEGER      │  ← 0=BUY, 1=SELL
│ lots        DECIMAL(10,2)│
│ open_price  DECIMAL(10,5)│
│ close_price DECIMAL(10,5)│
│ open_time   TIMESTAMP    │
│ close_time  TIMESTAMP    │
│ profit      DECIMAL(10,2)│
│ commission  DECIMAL(10,2)│
│ swap        DECIMAL(10,2)│
│ magic_number INTEGER     │
│ comment     TEXT         │
│ created_at  TIMESTAMP    │
└─────────────────────────┘

┌─────────────────────────┐
│      trade_diary         │
├─────────────────────────┤
│ id             SERIAL PK │
│ date           DATE UK   │  ← 1日1エントリー
│ market_comment TEXT       │
│ mental_state   VARCHAR(10)│  ← good / normal / bad
│ reflection     TEXT       │
│ next_day_plan  TEXT       │
│ created_at     TIMESTAMP │
│ updated_at     TIMESTAMP │
└─────────────────────────┘
```

### 4.2 インデックス

| テーブル | カラム | 理由 |
|---------|--------|------|
| trades | ticket | Webhookの重複チェック |
| trades | close_time | 期間検索の高速化 |
| trades | open_time | 期間検索の高速化 |
| trade_diary | date | 日付検索の高速化 |

---

## 5. 画面設計

### 5.1 ダッシュボード

```
┌──┬────────────────────────┬──────────────────┐
│  │ 日次損益グラフ          │  本日のサマリー   │
│  │ ┌────────────────────┐ │                  │
│  │ │                    │ │  損益: +12,450円 │
│  │ │  ▂ ▄ █ ▂ ▄ █ █ ▂  │ │  勝率: 62.5%    │
│  │ │  ▄ █ ▂ █ ▄ ▂ ▄ █  │ │  PF:   1.82     │
│  │ └────────────────────┘ │  取引: 8回       │
│  │                        │  平均: +1,556円  │
│  │ [本日] [今週] [今月]    │  最大DD: -3,200  │
│  │────────────────────────┼──────────────────│
│  │ 直近トレード            │  アラート        │
│  │ 09:15 BUY  +1,200     │  ⚠ 14時以降     │
│  │ 10:02 SELL  -800      │    ロット増加    │
│  │ 11:30 BUY  +2,400     │  ✅ 取引頻度OK  │
│  │ 13:45 SELL  +500      │                  │
└──┴────────────────────────┴──────────────────┘
```

### 5.2 トレード一覧

```
┌──┬───────────────────────────────────────────┐
│  │ フィルタ: [期間▼] [方向▼] [勝敗▼] [検索]  │
│  │─────────────────────────────────────────── │
│  │ # │ 時刻  │ 方向 │ Lot │ 損益   │ pips   │
│  │───┼───────┼──────┼─────┼────────┼────────│
│  │ 1 │ 09:15 │ BUY  │ 0.1 │+1,200 │ +12.0  │
│  │ 2 │ 10:02 │ SELL │ 0.1 │ -800  │  -8.0  │
│  │ 3 │ 11:30 │ BUY  │ 0.2 │+2,400 │ +12.0  │
│  │ 4 │ 13:45 │ SELL │ 0.1 │ +500  │  +5.0  │
│  │ 5 │ 14:22 │ BUY  │ 0.3 │-1,100 │  -3.7  │  ← ロット↑
│  │ 6 │ 15:01 │ SELL │ 0.3 │+3,200 │ +10.7  │  ← ロット↑
│  │─────────────────────────────────────────── │
│  │                    < 1 2 3 ... 10 >        │
└──┴───────────────────────────────────────────┘
```

### 5.3 AI分析

```
┌──┬───────────────────────────────────────────┐
│  │ 分析期間: [今週▼]    [▶ 分析を実行]       │
│  │─────────────────────────────────────────── │
│  │                                           │
│  │  📊 分析結果                               │
│  │                                           │
│  │  ⚠ ポジポジ病の兆候                       │
│  │  3月5日（水）の取引回数が12回で、          │
│  │  直近平均（4.2回）の約3倍です。            │
│  │                                           │
│  │  ⚠ ロットサイズの急変                      │
│  │  3月5日 14:22のトレードでロットが           │
│  │  0.1 → 0.3に増加しています。              │
│  │  直前の損失(-800円)の直後です。             │
│  │                                           │
│  │  ✅ 時間帯管理                              │
│  │  午前中（9:00-12:00）の勝率が78%と         │
│  │  安定しています。この時間帯に集中する        │
│  │  戦略は有効です。                          │
│  │                                           │
│  │  💡 総合コメント                            │
│  │  全体の勝率は62.5%で水準以上ですが、        │
│  │  水曜日に感情的なトレードの兆候が           │
│  │  見られます。大きな損失の直後は             │
│  │  10分以上間を空けることを推奨します。        │
│  │                                           │
└──┴───────────────────────────────────────────┘
```

### 5.4 経済指標カレンダー

```
┌──┬───────────────────────────────────────────┐
│  │ 表示: [今週▼]  重要度: [🔴高のみ▼]       │
│  │─────────────────────────────────────────── │
│  │                                           │
│  │  📅 3月3日（月）                           │
│  │  ────────────────────────────────────────  │
│  │  🟡 10:00  ISM製造業指数  前回:50.9       │
│  │                          予想:51.2       │
│  │                                           │
│  │  📅 3月5日（水）                           │
│  │  ────────────────────────────────────────  │
│  │  🔴 04:00  FOMC議事録公表                  │
│  │                                           │
│  │  📅 3月7日（金）                           │
│  │  ────────────────────────────────────────  │
│  │  🔴 22:30  米雇用統計（NFP）               │
│  │            前回:+353K  予想:+198K         │
│  │  🔴 22:30  失業率      前回:3.7%          │
│  │                        予想:3.7%         │
│  │                                           │
└──┴───────────────────────────────────────────┘
```

### 5.5 トレード日記

```
┌──┬───────────────────────────────────────────┐
│  │ [+ 新規作成]                    [月▼]     │
│  │─────────────────────────────────────────── │
│  │                                           │
│  │  📝 3月7日（金）      損益:+8,200 勝率:75% │
│  │  ┌─────────────────────────────────────┐  │
│  │  │ メンタル: 😊 良い                    │  │
│  │  │                                     │  │
│  │  │ 相場の感想:                          │  │
│  │  │ NFP前で慎重にトレードできた。         │  │
│  │  │ 午前中のレンジで3回スキャル成功。      │  │
│  │  │                                     │  │
│  │  │ 反省点:                              │  │
│  │  │ 指標直前にポジションを持ちかけた。     │  │
│  │  │ ルール通り見送れたのは良かった。       │  │
│  │  │                                     │  │
│  │  │ 翌日への意識:                         │  │
│  │  │ 週末なのでノートレード。              │  │
│  │  │ 来週の指標を確認しておく。            │  │
│  │  └─────────────────────────────────────┘  │
│  │                                           │
│  │  📝 3月6日（木）      損益:-2,100 勝率:40% │
│  │  ┌─────────────────────────────────────┐  │
│  │  │ メンタル: 😐 普通                    │  │
│  │  │ ...                                 │  │
│  │  └─────────────────────────────────────┘  │
│  │                                           │
└──┴───────────────────────────────────────────┘
```

---

## 6. データフロー

### 6.1 トレードデータの流れ

```
MT4でトレード決済
      ↓
EA（MQL4）がトレード情報をJSON化
      ↓
HTTP POST → FastAPI /api/trades/webhook
      ↓
FastAPIがバリデーション + 重複チェック（ticket番号）
      ↓
Supabase（PostgreSQL）に保存
      ↓
フロントエンドが GET /api/trades で取得して表示
```

### 6.2 AI分析の流れ

```
ユーザーが「分析を実行」ボタンを押す
      ↓
フロントエンド → POST /api/analysis（期間を指定）
      ↓
FastAPIが指定期間のトレードデータをDBから取得
      ↓
トレードデータ + 日記データを整形してプロンプトに組み込む
      ↓
Claude API に送信
      ↓
分析結果（テキスト）をフロントエンドに返却
      ↓
画面に表示
```

---

## 7. 技術選定の理由

| 技術 | 選定理由 |
|------|----------|
| React | コンポーネント指向でUI構築しやすい。学習リソースが豊富 |
| Vite | ビルドが高速。React公式推奨のビルドツール |
| TypeScript | 型があることでバグを未然に防げる。エディタの補完が効く |
| TailwindCSS | クラス名だけでスタイリング可能。CSSを別ファイルで管理する手間がない |
| shadcn/ui | 高品質なUIコンポーネント。必要なものだけ追加できる |
| Recharts | Reactと相性の良いグラフライブラリ |
| FastAPI | Pythonで高速なAPI構築が可能。自動ドキュメント生成 |
| SQLAlchemy | PythonのDB操作をオブジェクト指向で書ける |
| Supabase | PostgreSQLのホスティング。無料枠あり |
| Claude API | 日本語での自然な分析コメント生成が得意 |
